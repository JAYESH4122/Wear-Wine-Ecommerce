services:
  wearwine-db:
    container_name: wearwine-db
    image: postgres:16
    restart: always
    ports:
      - "5433:5432"

    environment:
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload
      POSTGRES_DB: wearwine

    volumes:
      - pgdata:/var/lib/postgresql/data

    logging:
      driver: none

volumes:
  pgdata: